{
  "name": "yapi-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Cursor plugin for YApi CLI workflows.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-template.mjs"
  }
}
